[project]
name = "neo"
version = "0.2.0"
description = "NEO SDK"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["cryptography>=46.0.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
